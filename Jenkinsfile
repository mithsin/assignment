pipeline {
    agent any
    stages {
        stage('init') {
            steps {
                bat 'npm install'
            }
        stage('start'){
                bat 'npm start'
            }
        }
    }
}
